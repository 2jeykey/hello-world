pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "CRON"'
                sh '''
                    echo "CRON"
                    ls -lah
                '''
            }
        }
    }
}
